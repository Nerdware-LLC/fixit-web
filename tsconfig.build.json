{
  "extends": "./tsconfig.json",

  "include": ["src"],

  // Exclude from build:
  "exclude": [
    // test and mock files
    "src/**/*.{test,spec}.[tj]s?(x)",
    "src/**/__mocks__/**/*",
    // storybook files
    "src/**/*.stories.[tj]s?(x)",
    "src/**/stories/**/*"
  ]
}
